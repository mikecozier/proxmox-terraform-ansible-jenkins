---
- name: Baseline config
  hosts: all
  become: true
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Install basics
      apt:
        name:
          - curl
          - git
          - htop
        state: present

